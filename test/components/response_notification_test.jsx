import { renderComponent, expect } from '../test_helper';
import ResponseNotification from '../../src/components/response_notification/response_notification';

/*eslint-disable */
describe('ResponseNotification', () => {
  let component;

  beforeEach(() => {
    component = renderComponent(ResponseNotification);
  });
});
